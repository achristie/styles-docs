machine:
  environment:
    USER_NAME: achristie
    USER_EMAIL: awchristie86@gmail.com
  node:
    version: 6.1.0
dependencies:
  post:
    - npm run build
deployment:
  pre:
    - git config user.name "achristie"
    - git config user.email "awchristie86@gmail.com"
  production:
    branch: master
    commands:
      - npm run deploy